const { DataTypes } = require("sequelize")
const db = require('../database/db.js')

const AyudanteModel = db.define('Ayudantias', {
  id_ayudantias:
  id_asignatura:
  id_usuario:
  cupos:
  horas:


})
